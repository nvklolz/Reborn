{% extends 'base.html.twig' %}

 {% block stylesheets %}
     {{ parent() }}
     <link rel="stylesheet" href="{{ asset('css/forum.css') }}">
     <style type="text/css">
         body {
             background: url('{{ asset('images/homepage/fond.jpg') }}') no-repeat top center #000000;
             color: #798896
         }
     </style>
 {% endblock %}
{% block body %}
    <header>
    </header>
    <div id="content">
        <div class="content-separator-title">
            <p>
                Forum Index
            </p>
        </div>
        <h4 class="head-titre-add-sujet">Recrutement</h4>
        <div class="section-forum">
            <div class="forum-category">
                <p>
                    Post a reply
                </p>
            </div>
        </div>
        {{ form_start(form) }}
        {{ form_errors(form) }}
        <div class="col-lg-7 col-new-sujet">
            <div class="form-group">
                {{ form_errors(form.content) }}
                {{ form_label(form.content, "Votre réponse") }}
                {{ form_widget(form.content, { 'attr': {'class': 'form-control'} }) }}
            </div>
            <div class="form-group">
                {{ form_widget(form.valider, { 'attr': {'class': 'btn btn-success'} }) }}
            </div>
            {{ form_end(form) }}
        </div>
    </div>
    <footer>
    </footer>
{% endblock %}